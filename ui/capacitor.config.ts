import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'macros.chat',
  appName: 'macros.chat',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
